import Link from "next/link"
import styles from "./banner.module.css"


const Banner = (inputText : string) => {
    return (
        <div className={styles.banner}>
        </div>

    );
}

export default Banner;